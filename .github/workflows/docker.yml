name: ci

on:
  push:
    branches: main

jobs:
  login:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name:
        run: |
            docker build . -t ${{secrets.DOCKERHUB_USERNAME}}/pruebas:lastest
      - name:
        run: |
            docker loging -u ${{secrets.DOCKERHUB_USERNAME}} -p {{secrets.DOCKERHUB_TOKEN}}
            docker push ${{secrets.DOCKERHUB_USERNAME}}/pruebas:lastest